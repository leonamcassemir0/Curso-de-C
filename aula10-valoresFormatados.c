#include <stdio.h>

int main(){
    // printf("%tipo_formatado", variavel_a_ser_imprimida);
    // printf("%i", x)

    int x = 11;
    printf("%i", x);

    /*
        %i OU %d será inteiro.
        %f para float OU double.
        %c para char.
        %s para string.

        %u módulo do número.
        %p endereço de memória.
        %e ou %E para notação científica.
        %% para imprimir o símbolo de porcentagem. 
    */

    return 0;
}